- alias: "Bedroom remote 1a on"
  trigger:
    - platform: state
      entity_id: switch.remote_1_a
      to: 'on'
  action:
    - service: switch.turn_on
      entity_id: switch.switch_bedroom

- alias: "Bedroom remote 1a off"
  trigger:
    - platform: state
      entity_id: switch.remote_1_a
      to: 'off'
  action:
    - service: switch.turn_off
      entity_id: switch.switch_bedroom


